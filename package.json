{
  "name": "lexi-app",
  "version": "1.0.0",
  "description": "Lexi - English to Hebrew Translator",
  "main": "www/index.html",
  "scripts": {
    "android": "cap add android && cap sync android",
    "build:android": "cap sync android && cd android && ./gradlew assembleRelease",
    "dev": "capacitor serve"
  },
  "dependencies": {
    "@capacitor/core": "^5.4.0",
    "@capacitor/android": "^5.4.0",
    "@capacitor/app": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.4.0"
  }
}